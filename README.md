# Responsive Video Editor

## Features
- Video trimming with start/end times.
- Responsive layout with Tailwind CSS.
- State management with Redux.

## Setup
`npm install` or `yarn install`
`npm start` or `yarn start`
